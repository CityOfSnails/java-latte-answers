public class RollerCoaster extends ConsoleProgram
{
    public void run()
    {
        boolean tallEnough = readBoolean("Are you tall enough to ride?");
        boolean oldEnough = readBoolean("Are you old enough to ride?");
        
        boolean canRide = tallEnough && oldEnough;
        
        System.out.println(canRide);
    }
}
